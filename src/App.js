import React, { Fragment } from 'react';
import { Normalize } from 'styled-normalize';
import Game from './Game';

const App = () => (
  <Fragment>
    <Normalize />
    <Game />
  </Fragment>
);

export default App;
