import React from 'react';
import HeroContainer from './HeroContainer';

const Hero = () => (
  <HeroContainer>
    <section>
      <h1>EPL Memory Game!</h1>
      <p>
        Choose a team to score points, but don't choose any team more than once!
      </p>
    </section>
  </HeroContainer>
);

export default Hero;
